{
  "name": "@shadr/editor",
  "version": "0.0.0",
  "description": "A customizable node editor for Shadr",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "pixi.js": "^7.3.2"
  },
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec npm run build",
    "clean": "rimraf dist",
    "build": "npm run clean && tsc"
  },
  "author": "RATIU5 <contact@ratiu5.dev>",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.2",
    "typescript": "^5.3.3"
  }
}
