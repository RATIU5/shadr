{
  "name": "@shadr/editor-grid",
  "version": "0.0.1",
  "private": true,
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "watch": "tsup --watch",
    "dev": "tsup --watch",
    "lint": "eslint src/",
    "clean": "rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@shadr/editor-events": "workspace:*",
    "@shadr/editor-core": "workspace:*",
    "pixi.js": "^8.5.2"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "@shadr/common": "workspace:*"
  }
}
