{
  "name": "@shadr/devtools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w --preserveWatchOutput",
    "typecheck": "tsc -b",
    "lint": "eslint --fix .",
    "test": "vitest --run --config ../../vitest.config.ts --root ../.. --include packages/devtools/test/**/*.test.ts"
  }
}
