{
  "name": "@shadr/plugin-system",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w --preserveWatchOutput",
    "typecheck": "tsc -b",
    "lint": "eslint --fix .",
    "test": "vitest --run --config ../../vitest.config.ts --root ../.. --include packages/plugin-system/test/**/*.test.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "dependencies": {
    "effect": "catalog:effect",
    "@shadr/shared": "workspace:*"
  }
}
