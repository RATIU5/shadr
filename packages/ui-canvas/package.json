{
  "name": "@shadr/ui-canvas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w --preserveWatchOutput",
    "typecheck": "tsc -b",
    "lint": "eslint --fix .",
    "test": "vitest --run --config ../../vitest.config.ts --root ../.. --include packages/ui-canvas/test/**/*.test.ts"
  },
  "dependencies": {
    "@shadr/graph-core": "workspace:*",
    "pixi.js": "^8.6.1"
  }
}
