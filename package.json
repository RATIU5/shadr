{
  "name": "shadr",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^8.0.0"
  },
  "dependencies": {
  }
}
