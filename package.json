{
  "name": "root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "effect-language-service patch",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "typecheck": "tsc --no-emit"
  },
  "devDependencies": {
    "@effect/language-service": "^0.69.11",
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3"
  }
}
